This directory is intentionally not a valid DuckDB extension repository.

It is used by the Irion extension-security SQLLogicTest as a local repository
target on both Linux and Windows. The test verifies that DuckDB does not fall
back to a public repository and that community extension loading is disabled.
