-- Install the official extensions once in the isolated HOME used by the CI job.
INSTALL json;
INSTALL tpch;
INSTALL tpcds;
INSTALL icu;
INSTALL httpfs;
INSTALL ducklake;
INSTALL postgres_scanner;
INSTALL sqlite_scanner;
